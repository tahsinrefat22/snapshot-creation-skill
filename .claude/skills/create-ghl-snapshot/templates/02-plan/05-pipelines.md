# Pipelines — <business>

Defaults-first: the default pipeline (if present, per `00-baseline-inventory.md`) is used/renamed before a new one is created.

## <Pipeline name>

Purpose: … · Customer types using it: …

| #   | Stage | Contact enters when | Moved by (workflow / manual) | Leaves when | Won / Lost? |
| --- | ----- | ------------------- | ---------------------------- | ----------- | ----------- |

Dashboard widgets fed: …

## Ledger rows

<!-- type pipeline + one row per stage · origin mine / pre-existing -->
