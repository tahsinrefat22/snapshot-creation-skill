# Tags — <business>

Convention: `state:<x>` = where the contact is now (removed when no longer true) · `hist:<x>` = something happened (permanent) · `src:<x>` = source. Never filter "currently in X" on a `hist:` tag.

| Tag            | Kind | Purpose                             | Applied by | Removed by | Read by (trigger filter / condition) | Defaults-first justification (why not a field / stage / built-in) |
| -------------- | ---- | ----------------------------------- | ---------- | ---------- | ------------------------------------ | ----------------------------------------------------------------- |
| `test-contact` | hist | marks Phase 4 test data for cleanup | Phase 4    | Phase 6    | —                                    | —                                                                 |

## Ledger rows

<!-- type tag · origin mine · status planned -->
