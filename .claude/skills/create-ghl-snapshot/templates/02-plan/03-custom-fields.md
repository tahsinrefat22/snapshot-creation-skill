# Custom fields — <business>

Defaults-first: built-in contact fields (`reference/defaults-first.md` → built-in list, stamped) are used before any custom field. Field types come from `ghl-capabilities.md` → `custom-fields` (stamped).

| Name | Type (verified) | Object                | Written by                    | Read by                                   | Only exists to feed a workflow condition? | Defaults-first justification |
| ---- | --------------- | --------------------- | ----------------------------- | ----------------------------------------- | ----------------------------------------- | ---------------------------- |
|      |                 | contact / opportunity | form / survey / workflow step | workflow condition / template / dashboard | yes / no                                  |                              |

## Ledger rows

<!-- type custom-field · origin mine · status planned -->
