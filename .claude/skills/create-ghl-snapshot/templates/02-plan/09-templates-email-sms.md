# Email and SMS templates — <business>

Every body uses variables for business identity. SMS carries the opt-out phrase if `ghl-capabilities.md` / docs say GHL doesn't add it automatically (record which).

## SMS: <name>

Sent by: workflow <name> step <n> · Customer types: … · Timing: …

```
Hi {{contact.first_name}}, this is {{location.name}}. …
```

Variable map: | Spot | Variable |
Length note: <segments>

## Email: <name>

Sent by: … · Subject: … · Preheader: …
Body outline (sections + copy with variables): …
Variable map: …

## Ledger rows

<!-- type template-sms / template-email -->
